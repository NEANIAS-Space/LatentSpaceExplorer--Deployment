apiVersion: v1
kind: ConfigMap
metadata:
  name: client-config
  labels:
    k8s-app: server
data:
  .env: |-
    APP_SERVER_HOST=0.0.0.0
    APP_SERVER_PORT=8080
    APP_SERVER_ROOT_PATH=/server
    APP_SERVER_RELOAD=True

    NEXCLOUD_HOST=<>
    NEXCLOUD_USER=<>
    NEXCLOUD_PASSWORD=<>
