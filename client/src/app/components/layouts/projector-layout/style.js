import styled from 'styled-components';

const ProjectorLayoutWrapper = styled.div`
    height: 100vh;
    display: grid;
    grid-template-rows: 64px auto;
    grid-template-columns: 20% auto 20%;
    overflow: hidden;
`;

export default ProjectorLayoutWrapper;
